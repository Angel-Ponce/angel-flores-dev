---
import { Icon } from 'astro-icon/components';
import { v4 as uuidV4 } from 'uuid';

const buttonId = `button-${uuidV4()}`;
---

<button
  class:list={['group cursor-pointer']}
  id={buttonId}
  onclick="window.toggleTheme()"
>
  <Icon name="duo-icons:sun" size={20} />
</button>
